------------------------------------------------------------------------
-- RotaAssist - Transition Matrix: Rogue / Subtlety (specID 261)
-- Empty shell default matrix (awaiting training pipeline fill)
------------------------------------------------------------------------

local _, NS = ...
local RA = NS.RA

RA.TransitionMatrix = RA.TransitionMatrix or {}
RA.TransitionMatrix[261] = {
    matrix = {}
}
