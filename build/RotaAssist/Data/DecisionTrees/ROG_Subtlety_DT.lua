------------------------------------------------------------------------
-- RotaAssist - Decision Tree: Rogue / Subtlety (specID 261)
-- Empty shell implementation (awaiting training pipeline fill)
------------------------------------------------------------------------

local _, NS = ...
local RA = NS.RA

RA.DecisionTrees = RA.DecisionTrees or {}
RA.DecisionTrees[261] = { 
    Evaluate = function(f) 
        return nil 
    end 
}
